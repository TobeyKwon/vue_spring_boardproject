spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: kwon
    password: 1234
    url: jdbc:mysql://localhost:3306/blog?serverTimezone=Asia/Seoul&useSSL=false?allowPublicKeyRetrieval=true"
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    database: mysql
    properties:
      hibernate.default_batch_fetch_size: 1000
