spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: kwon
    password: 1234
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=utf8&allowPublicKeyRetrieval=true&useSSL=false
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        default_batch_fetch_size: 1000
    database: mysql
  messages:
    basename: messages,errors
